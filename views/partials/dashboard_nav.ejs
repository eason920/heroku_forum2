<nav class="navbar navbar-expand-md navbar-dark bg-dark sticky-top">
	<a class="navbar-brand mr-2" href="/dashboard/archives">
		<i class="fa fa-diamond" aria-hidden="true"></i>
		 徐琮偉 Eason 部落格：控制台
	</a>
	<a class="navbar-brand" href="/contact">| 與我聯擊</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
		aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
		<div class="navbar-nav">
			<a class="nav-item nav-link <% if(nav === 'archives'){ %>active<% } %>" href="/dashboard/archives">文章管理 </a>
			<a class="nav-item nav-link <% if(nav === 'categories'){ %>active<% } %>" href="/dashboard/categories">分類管理 </a>
		</div>
	</div>
	
	<% if(auth){ %>
		<a class="nav-link" id='logout' href="#">登出</a>
	<% }else{ %>
		<a class="nav-link" href="/auth/login">登入</a>
	<% } %>
</nav>

<script>
	$('#logout').on('click', function(e){
		e.preventDefault();
		$.ajax({
			url: '/auth/logout',
			type: 'post'
		}).done(function(){
			window.location = '/forum';
		})
	});
</script>
